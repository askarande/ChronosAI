pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PRESERVE_EXISTING)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "ChronosAI"
include ':app'
